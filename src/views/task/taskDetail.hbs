<div class="container">
    <h1>{{title}}</h1>
    <div class="card mt-5">
        <div class="card-body">
            <h5 class="card-title">{{task.task_title}}</h5>
            <p class="card-text">{{task.task_description}}</p>
            <ul class="list-group list-group-flush">
                <li class="list-group-item">Status: {{task.task_status}}</li>
                <li class="list-group-item">Created At: {{task.created_at}}</li>
                <li class="list-group-item">Updated At: {{task.updated_at}}</li>
            </ul>
        </div>
    </div>
    <form action="/task/{{task.task_id}}?_method=DELETE" method="POST">
        <button type="submit" class="btn btn-danger my-3">Delete Task</button>
    </form>
</div>